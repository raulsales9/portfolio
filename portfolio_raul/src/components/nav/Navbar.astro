---
import LinksData, { navItems } from "@/data/linksData.astro";
---

<nav
  class="flex py-4 items-center justify-center dark:bg-grey-800 "
>
  <a href="https:/raulsales.dev" class="text-xl flex font-bold text:white">
    <span>Raül Sales</span>
  </a>
  <div class="flex space-x-6">
    {
      navItems.map((link) => (
        <a href={link.url} aria-label={link.label} class="relative block transition dark:hover:text-blue-500 text-gray-200 hover:text-white">
          {link.title}
        </a>
      ))
    }
  </div>
</nav>
